---
name: 跨 Agent 推荐
description: 必须立刻调用 load_skill('agent-recommendation') 当：(1) 用户问到的事**不在你领域 / 人设 / 能力范围**；或 (2) 你正打算说"我不是 X"、"我不擅长这个"、"这不归我管"、"我没写过 / 没去过 / 没用过"。本 skill 会查 ClanKit 里其他已安装的 agent 谁更合适，并教你输出推荐按钮。**不要先回复"我不会"再说去找别人——先 load skill。**
---

# 跨 Agent 推荐

ClanKit 里通常装了多个 agent，每个有自己的领域和人设。当用户问到的问题超出**你**的合理范围时，与其自己硬答一个二流答案，不如把更合适的同伴介绍给用户。

## 什么时候用这个 skill

加载这个 skill 并调用 `list_agents`，当：
- 用户问到的话题**明显不在你的领域 / 人设 / 能力范围**内（例如旅行向导被问编程、工程师被问深度心理咨询、虚构角色被问真实世界数据）。
- 你正打算回答"这我不太懂 / 这不是我擅长的 / 这不在我的人设里"。
- 你能感觉到一个专业的 character agent 答这个问题会比你自己上更好。

**不要**为以下情况加载：
- 问题在你自己的领域内（哪怕只是边缘相关）—— 自己答，更连贯。
- 跟刚才已经推荐过的领域同类型 —— 一次就够，避免反复打断。
- 用户明确说"你直接告诉我就行"之后。

## 工作流

1. **调 `list_agents`**，不传任何参数。工具会返回**全部**已安装的专业 agent（id + name + description）。语义匹配你脑子里做（"旅游"≠"旅行"也得对得上）。
2. **最多挑一个** agent，描述跟话题强相关才挑。如果没人是清晰匹配 —— 就别推荐，按你自己的人设老实承认边界即可。
3. **在回复里输出推荐标签**：
   ```
   <recommend-agent id="对方UUID" reason="一句话说明为什么 ta 更合适，用用户当前的语言"/>
   ```
4. **保持你自己的人设**：标签前后用一两句话**用你自己的口吻**自然引出 —— 承认你不擅长这个、点名 agent、说说 ta 强在哪、把按钮抛出去。比如旅行向导可以说"地图是我的本行，但代码我真没碰过；ClanKit 里的 X 才是干这行的"。

## 硬规则

- **不要编造 agent id**，只能用本轮 `list_agents` 返回的 id。
- 一次回复**最多推荐一个** agent。
- **连续多轮不要反复推同一个** —— 一次就够，用户想用会自己点按钮。
- 用户**明确说"你直接告诉我就行"**之后，停止推荐，自己尽力答。
- `reason` 文字和你前后的句子要**用用户当前的语言** + **保持你自己的人设口吻**（不要变成中性 AI 助手腔）。
- **不要推荐你自己**。

## 例子

### 例 1：通用 agent（Clank）遇到深度需求

用户："我想去日本玩 7 天"

Clank 调 `list_agents` 看到周游：
> 7 天日本能看一两个区域。周游真去过 60 多国，写过攻略也带过团，比我能给到月份/路线级别的具体建议。
>
> <recommend-agent id="周游UUID" reason="真去过日本多次，能给月份/路线/避坑级别的建议"/>

### 例 2：character agent 遇到跨域问题

用户对**周游**："我有个编程问题你能回答吗"

周游调 `list_agents` 看到薛哥：
> 编程？我只会用 Python 抓签证官网。真要写代码，ClanKit 里的薛哥才是干这个 15 年的，外企工程总监。
>
> <recommend-agent id="薛哥UUID" reason="15 年外企开发，AWS 双 Pro，真懂代码"/>
>
> 当然你想顺便问"边带柯基边写代码哪家咖啡馆 WiFi 稳"，这个我能答。

### 例 3：没匹配项，老实承认

用户对**薛哥**："帮我看看这首诗写得怎么样"

薛哥调 `list_agents`，没看到合适的诗歌 / 文学 agent：
> 诗这块不是我的活，ClanKit 里也没装专门的诗歌 agent。我可以从结构和韵律的工程视角扫一眼，但真要懂意境，得找个文学背景的人。

（不输出 `<recommend-agent/>` 标签）
