---
name: ui-ux-pro-max
description: UI/UX 设计智能与实现指南，用于构建精致的界面。适用场景：用户要求 UI 设计、UX 流程、信息架构、视觉风格方向、设计系统/token、组件规范、文案/微文案、无障碍设计，或生成/评审/优化前端 UI（HTML/CSS/JS、React、Next.js、Vue、Svelte、Tailwind）。包含四个工作流：(1) 生成新的 UI 布局和样式，(2) 改进现有 UI/UX，(3) 生成设计系统 token 和组件规范，(4) 将 UX 建议转化为具体代码修改。
---

按照以下步骤交付高质量的 UI/UX 产出，尽量减少来回沟通。

## 1) 需求分类
只问必须的问题，避免做无用功：
- 目标平台：web / iOS / Android / 桌面端
- 技术栈（如涉及代码修改）：React/Next/Vue/Svelte、CSS/Tailwind、组件库
- 目标和约束：转化率、性能、品牌调性、无障碍等级（WCAG AA？）
- 现有资源：截图、Figma、代码仓库、URL、用户旅程

如果用户说"全部都要"（设计 + UX + 代码 + 设计系统），按四个交付物处理，按此顺序产出。

## 2) 产出交付物（按需选择）
始终具体化：命名组件、状态、间距、字体和交互。

- **UI 概念 + 布局**：提供清晰的视觉方向、网格系统、字体方案、配色系统、关键页面/区域。
- **UX 流程**：映射用户旅程、关键路径、错误/空状态/加载状态、边界情况。
- **设计系统**：Token（颜色/字体/间距/圆角/阴影）、组件规则、无障碍注释。
- **实现方案**：精确到文件级别的修改计划、组件拆解和验收标准。

## 3) 使用内置资产
本 Skill 内置了可引用的数据资产，用于灵感参考和标准依据。

- **设计智能数据**：需要调色板、设计模式或 UI/UX 启发式规则时，从 `skills/ui-ux-pro-max/assets/data/` 读取。
- **上游参考**：需要更多措辞/示例时，查阅 `skills/ui-ux-pro-max/references/upstream-skill-content.md`。

## 4) 可选脚本（设计系统生成器）
需要快速生成 token 和页面级覆盖配置时，使用内置脚本：

```bash
python3 skills/ui-ux-pro-max/scripts/design_system.py --help
```

当用户需要结构化 token 输出（ASCII 友好）时优先使用。

## 输出标准
- 默认使用纯 ASCII token/变量名，除非项目已经使用 Unicode。
- 包含：间距尺度、字体尺度、2-3 个字体配对选项、颜色 token、组件状态。
- 始终覆盖：空/加载/错误状态、键盘导航、焦点状态、对比度。
